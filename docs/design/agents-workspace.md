# design：Agents 工作区

## 原则

- **对话为主**：主区域承载与 Agent 的持续交互；控制项尽量不淹没对话流。
- **上下文外置**：与任务相关的目录、提示、能力等与侧栏聚合，用户可按需展开。
- **项目作用域**：在 Butler / 工作区模式下，`projectKey` 等注入应对用户可理解（与实现细节见 as-is）。

## 目标态（可与现状有差距）

- 侧栏与会话列表、文档索引等能力的深度整合（实现进度以 as-is / contracts 为准）。

## 相关现状

- `docs/as-is/agents-workspace.md`
