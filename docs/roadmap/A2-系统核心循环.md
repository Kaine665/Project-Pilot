# A2 — 系统核心循环写入 §1

## 背景

`领域与数据.md` §1 把五关注面列成平行表格，读起来像五块散的砖头，看不出它们是一台机器。
需要在表格**之前**加一段「系统核心循环」，让读者先有整体图，再看各段细节。

## 当前状态

✅ 已写入 `develop-static/docs/领域与数据.md` §1（`### 核心循环` + 流水线 + 环节表 + 「下表列出…」）。

## 方案

在 §1 标题和表格之间插入以下内容：

```markdown
### 核心循环

五关注面不是并列的五块，而是一条流水线上的五段：

​```
触发（③）→ 寻址（①）→ 给料（④）→ 执行记录（②）→ 结果追踪（⑤）
​```

| 环节 | 对应关注面 | 系统在做什么 |
|------|-----------|-------------|
| 有件事来了 | **③ Trigger** | 把各种来源（人/定时/事件）统一成一条消息 |
| 该谁做、在哪做 | **① Scope** | 定位 Project → Agent → Session |
| 带着什么去做 | **④ Resource** | 把指令、知识、工具拼到上下文里 |
| 做的过程 | **② Execution** | 每一步留痕（Event），有目标的尝试标为 Run |
| 做完了怎么样 | **⑤ Tracking** | 这件事完成了吗、进度如何 |

Scope（①）贯穿全程——每一步都要用它来确定「我在哪、我能看到什么」。

下表列出五关注面的具体概念：
```

保留原表格和交叉规则不动。

## 改动范围

- `develop-static/docs/领域与数据.md` §1

## 验收标准

- §1 先出现流水线图 + 环节表，再出现五关注面详细表格
- 读者先看到整体，再看各段

## 讨论记录

- 2026-04-02：讨论确定，五关注面应表达为有因果顺序的流水线而非平铺切面
- 2026-04-02：已落盘至 `领域与数据.md` §1
