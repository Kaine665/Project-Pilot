# as-is（现状文档）

描述**当前**实现：用户可见行为、入口文件、主要依赖、持久化边界。文内用**仓库相对路径**链到代码（单向）。

## 维护

- 在 **契约交付** 或 **改变行为的合并** 后更新对应页，并刷新 `last_reviewed`（ISO 日期）。
- 不要求与每个 commit 同步。

## 已有页面

| 文档 | 覆盖能力域 |
|------|------------|
| [agents-workspace.md](./agents-workspace.md) | Flows · Agents 工作区布局与侧栏 |
