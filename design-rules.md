## 设计知识的记录规范

对于涉及架构/设计层面的决策，必须在以下四处同步记录，防止后续被误改：

1. **函数命名** — 名字本身就要体现设计意图（如 `fetchGeneralDefinition` VS `fetchContextualDefinition`）
2. **代码旁注释** — 在调用处写结构注释块，说明各层关系和"不可替代"的约束
3. **MEMORY.md** — 给 AI 下次进项目时快速获取上下文，写关键结论而非细节
4. **docs/** — 给人类读的完整架构文档，含流程图、表格、文件清单
