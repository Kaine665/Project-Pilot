# Shared UI primitives (docs-relevant)

The 文档 page uses **native HTML + Tailwind** for forms and list UI. Shell uses:

## `src/components/sidebar-icon-button.tsx`

40×40 icon button with tooltip; active/inactive zinc styles.

## `src/components/ui/tooltip.tsx`

Radix tooltip (used by sidebar buttons).

Full source: read from repository when extracting components; pass as `--context-file` for drafts.
