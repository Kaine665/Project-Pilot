# Theme — `src/app/globals.css` (excerpt)

Tailwind v4: `@import "tailwindcss"`, `@theme inline` maps semantic colors to CSS variables. `:root` and `.dark` define oklch neutrals + hex semantic accents (`--color-ai`, `--color-user`, …). Body: `bg-background text-foreground`, `font-family: system-ui, -apple-system, sans-serif`.

**Pass full file as context**: `src/app/globals.css`
